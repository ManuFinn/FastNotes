<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm ="clr-namespace:LoginApp.ViewModels"
             x:Class="LoginApp.Views.ListaProductosView">
    <ContentPage.BindingContext>
        <vm:ProductosViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Bienvenido usuario"></Label>
            <Label Text="{Binding Error}"></Label>
            <CollectionView ItemsSource="{Binding Lista}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame>
                            <StackLayout>
                                <Label Text="{Binding NombreProducto}"/>
                                <Label Text="{Binding PrecioProducto}"/>
                                <Label Text="{Binding TipoProducto}"/>
                            </StackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>